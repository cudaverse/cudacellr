% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sce-adapter.R
\name{cudacell_sce}
\alias{cudacell_sce}
\title{Run a cudacell workflow on a SingleCellExperiment}
\usage{
cudacell_sce(
  x,
  assay = "counts",
  n_hvg = 2000L,
  n_components = 30L,
  k = 15L,
  device = c("auto", "cuda", "cpu"),
  batch_size = 256L,
  scale_factor = 10000,
  log1p = TRUE,
  min_mean = 0,
  scale. = TRUE,
  normalized_assay = NULL,
  reduced_dim = "CUDACELL_PCA",
  col_pair = "CUDACELL_KNN",
  row_data_prefix = "cudacell",
  set_size_factors = FALSE,
  overwrite = FALSE,
  realize = FALSE
)
}
\arguments{
\item{x}{A \code{SingleCellExperiment}.}

\item{assay}{Exact name of the feature-by-cell count assay.}

\item{n_hvg}{Number of highly variable features.}

\item{n_components}{Number of PCA components.}

\item{k}{Number of neighbours per cell.}

\item{device}{Computation device.}

\item{batch_size}{Maximum query rows per exact kNN distance block.}

\item{scale_factor}{Target library size used during normalization.}

\item{log1p}{Whether to apply \code{log1p()} after library-size normalization.}

\item{min_mean}{Minimum feature mean used for HVG selection.}

\item{scale.}{Whether to scale selected features before PCA.}

\item{normalized_assay}{Name for the added normalized assay. \code{NULL} chooses
\code{"cudacell_logcounts"} when \code{log1p = TRUE} and
\code{"cudacell_normalized"} otherwise.}

\item{reduced_dim}{Name for the added PCA reduced dimension.}

\item{col_pair}{Name for the added directed kNN \code{colPair}.}

\item{row_data_prefix}{Prefix for added feature statistics and the
namespaced column size factor.}

\item{set_size_factors}{Whether to also replace the canonical
\code{\link[SingleCellExperiment:sizeFactors]{SingleCellExperiment::sizeFactors()}} values. The namespaced
\verb{<row_data_prefix>_size_factor} column is always added to \code{colData}.}

\item{overwrite}{Whether explicitly named cudacellr output fields may be
replaced. Other object content is never overwritten.}

\item{realize}{Whether a delayed assay may be explicitly materialized as a
sparse \code{Matrix}.}
}
\value{
A valid \code{SingleCellExperiment} of the same class as \code{x}.
}
\description{
\code{cudacell_sce()} reads one assay, runs \code{\link[=cudacell_workflow]{cudacell_workflow()}}, and returns a
modified copy of the input object. Existing assays, row and column
metadata, reduced dimensions, alternative experiments, pairings, and
top-level metadata are retained. New results are stored in native
\code{SingleCellExperiment} locations:
}
\details{
\itemize{
\item normalized expression in an assay;
\item PCA scores in a reduced dimension;
\item highly variable feature statistics in \code{rowData};
\item directed nearest-neighbour relationships in a \code{colPair};
\item parameters and compute provenance in \code{metadata(x)$cudacellr}.
}

The selected assay must currently be a base matrix or \code{Matrix} object.
Delayed assays are never realized silently. Set \code{realize = TRUE} only after
confirming that the selected assay fits in memory.
}
\examples{
if (requireNamespace("SingleCellExperiment", quietly = TRUE)) {
  counts <- matrix(
    rpois(30 * 12, lambda = 2),
    nrow = 30,
    dimnames = list(paste0("gene_", 1:30), paste0("cell_", 1:12))
  )
  sce <- SingleCellExperiment::SingleCellExperiment(
    assays = list(counts = counts)
  )
  sce <- cudacell_sce(
    sce,
    n_hvg = 10,
    n_components = 3,
    k = 3,
    device = "cpu"
  )
  SingleCellExperiment::reducedDim(sce, "CUDACELL_PCA")
  cuda_provenance(sce)
}
}
