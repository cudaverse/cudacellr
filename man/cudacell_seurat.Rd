% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seurat-adapter.R
\name{cudacell_seurat}
\alias{cudacell_seurat}
\title{Run a cudacell workflow on a Seurat object}
\usage{
cudacell_seurat(
  x,
  assay = NULL,
  layer = "counts",
  n_hvg = 2000L,
  n_components = 30L,
  k = 15L,
  device = c("auto", "cuda", "cpu"),
  batch_size = 256L,
  scale_factor = 10000,
  log1p = TRUE,
  min_mean = 0,
  scale. = TRUE,
  output_assay = "CUDACELL",
  reduction = "cudacell_pca",
  reduction_key = "CUDACELLPC_",
  neighbor = "cudacell_knn",
  metadata_prefix = "cudacell",
  overwrite = FALSE,
  realize = FALSE
)
}
\arguments{
\item{x}{A \code{Seurat} object.}

\item{assay}{Exact input assay name. \code{NULL} uses
\code{\link[SeuratObject:DefaultAssay]{SeuratObject::DefaultAssay()}}.}

\item{layer}{Exact feature-by-cell count layer name.}

\item{n_hvg}{Number of highly variable features.}

\item{n_components}{Number of PCA components.}

\item{k}{Number of neighbours per cell.}

\item{device}{Computation device.}

\item{batch_size}{Maximum query rows per exact kNN distance block.}

\item{scale_factor}{Target library size used during normalization.}

\item{log1p}{Whether to apply \code{log1p()} after library-size normalization.}

\item{min_mean}{Minimum feature mean used for HVG selection.}

\item{scale.}{Whether to scale selected features before PCA.}

\item{output_assay}{Name for the added normalized \code{Assay5}. Its \code{data}
layer contains normalized expression.}

\item{reduction}{Name for the added PCA \code{DimReduc}.}

\item{reduction_key}{Seurat key for PCA dimensions. It must start with a
letter, contain only letters or digits, and end in an underscore.}

\item{neighbor}{Name for the added exact kNN \code{Neighbor}.}

\item{metadata_prefix}{Prefix for feature statistics and the cell-level
size-factor column.}

\item{overwrite}{Whether the named cudacellr output fields may be replaced.
Other object state is never overwritten.}

\item{realize}{Whether a non-memory-backed layer may be explicitly
materialized as a sparse \code{Matrix}.}
}
\value{
A valid \code{Seurat} object. The input object is not modified.
}
\description{
\code{cudacell_seurat()} reads one exact assay layer, runs
\code{\link[=cudacell_workflow]{cudacell_workflow()}}, and returns a modified copy of the input object.
The full Seurat package is not required; the object contract uses
\code{SeuratObject} version 5 or newer.
}
\details{
Results are stored in native, namespaced locations:
\itemize{
\item normalized expression and feature statistics in a new \code{Assay5};
\item PCA embeddings and loadings in a \code{DimReduc};
\item exact kNN indices and distances in a \code{Neighbor};
\item library-size factors in cell metadata;
\item parameters and compute provenance in the \code{cudacell_seurat} tool record.
}

Existing assays and layers, reductions, graphs, neighbours, identities,
cell metadata, images, commands, miscellaneous data, and tool records are
retained. Every output collision is checked before device selection or
computation. \code{overwrite = TRUE} replaces only the explicitly named
cudacellr outputs.

Feature metadata use the requested prefix. \verb{<prefix>_hvg_rank} is the
one-based global dispersion rank across all input features;
\verb{<prefix>_hvg} identifies the selected top \code{n_hvg} features.

Non-memory-backed layers are never realized silently. Set \code{realize = TRUE}
only after confirming that the selected layer fits in memory. The selected
layer must contain every object cell in the same order.
}
\examples{
if (requireNamespace("SeuratObject", quietly = TRUE)) {
  counts <- Matrix::Matrix(
    matrix(
      rpois(30 * 12, lambda = 2),
      nrow = 30,
      dimnames = list(
        paste0("gene_", 1:30),
        paste0("cell_", 1:12)
      )
    ),
    sparse = TRUE
  )
  object <- SeuratObject::CreateSeuratObject(counts)
  object <- cudacell_seurat(
    object,
    n_hvg = 10,
    n_components = 3,
    k = 3,
    device = "cpu"
  )
  SeuratObject::Embeddings(object[["cudacell_pca"]])
  cuda_provenance(object)
}
}
